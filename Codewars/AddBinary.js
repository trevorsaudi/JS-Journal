// a function that adds two numbers together and returns their sum in binary
function AddBinary(a,b){
    return (a+b).toString(2);
}

console.log(AddBinary(4,6));

// for javascript, passing a value inside the toString method is used as the base for the operation